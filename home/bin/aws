#!/bin/bash
ARGS="$@"

if [ -z "$DOT_AWS" ]; then
    DOT_AWS=$HOME/.aws
fi

AWS=/usr/local/bin/aws-v2
#AWS=/usr/local/bin/aws-v1

TMPD=/tmp/x
mkdir -p $TMPD

docker run -t -i --rm -v $TMPD:$TMPD -v $DOT_AWS:/home/dev/.aws aws-tools:latest bash -c "$AWS $ARGS"
