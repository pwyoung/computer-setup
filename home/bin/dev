#!/bin/bash

# On windows
# ln -s '/cygdrive/c/Users/youngp/AppData/Local/Mozilla Firefox/firefox.exe' ~/bin-local/firefox

test_docker() {
    docker rm -f nginx
    docker run --name=nginx -d -v ~/nginxlogs:/var/log/nginx -p 5000:80 nginx
    firefox http://localhost:5000
    docker rm -f nginx
}

run_dev_container() {

    # Uncomment for initial dev/test
    #docker rm -f dev

    #docker run --name=dev -d nginx 
    #docker run -it ubuntu bash
    #docker run --name=dev -it ubuntu bash
    #docker run --name=dev -it ubuntu:18.04 bash    
    #docker run --name=dev -it ubuntu:19.10 bash        
    # docker run --name=dev -it ubuntu:18.04 bash -c 'ls /'
    # docker run --rm -v c:/Users/youngp/'OneDrive - ACI Worldwide Corp'/data:/data alpine ls /data
    #docker run --rm -v c:/Users/youngp/'OneDrive - ACI Worldwide Corp'/data:/data ubuntu:18.04 ls /data   
    docker run --rm -v c:/Users/youngp/'OneDrive - ACI Worldwide Corp'/data:/data ubuntu:18.04 bash

}

# test_docker
run_dev_container

